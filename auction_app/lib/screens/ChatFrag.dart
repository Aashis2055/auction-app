import 'package:flutter/material.dart';

class ChatFrag extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Chat Fragment'),
    );
  }
}
